package com.chinmayshivratriwar.dev_playground.dsa;

public interface BinaryHeap {
    //Binary heap is a complete binary tree where each node is smaller than its children (min-heap) or larger than its children (max-heap)
    //Filled from left to right at each level
    // left (i) = 2i + 1
    // right (i) = 2i + 2
    // parent (i) = (i - 1) / 2
}
